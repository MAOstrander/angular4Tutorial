import { CharTextDirective } from './char-text.directive';

describe('CharTextDirective', () => {
  it('should create an instance', () => {
    const directive = new CharTextDirective();
    expect(directive).toBeTruthy();
  });
});
